package cn.ynou.casmanagement.repository;

import cn.ynou.casmanagement.model.UserGroupMappings;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

//public interface UserGroupMappingsRepository {
//public interface UserGroupMappingsRepository extends JpaRepository<UserGroupMappings,UserGroupMappings.UserGroupMappingsKey> {
//    public List<UserGroupMappings> findByKeyContains(Integer groupId);
////    public List<UserGroupMappings> findByGroupId(Integer groupId);
//}
